## 3.9

修改 physgto 文件，尝试直接预测增量，不再乘以 dt

## 3.11

* 加入速度映射和死区操作 （dataset），通过配置文件中的 log_velocity 控制；根据选择的txt数据文件专门计算均值和方差； 

    暂时采用的公式为 
    ````math
    y = \text{sign}(x) \cdot \ln(1 + 1000 \cdot |x|)
    ````

* 对现有的数据集进行裁剪，正好包含所有的关键区域（所有有变化的物理场区域），裁剪不会影响目前传入的物理量。

* LNN 加入

## 3.12 
* 2D 的预测效果好于 3D；但是 alpha.air 场在不活跃区的预测误差远大于 温度场，拟尝试如下方案改善：

    * 对组分场（范围 0-1）采用 BCE + Dice Loss 替代
    * 对组分场采用类似于温度场的加权（活跃区加权）
    * 课程学习 Curriculun Training：依次学习

## 3.15
* 修改原有在 dataset 中直接 mask 掉；
* 映射函数调整（可能）